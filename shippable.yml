language: java
jdk:
  - openjdk6
  - oraclejdk7
  - openjdk7
cache:
  directories:
    - $HOME/.m2
before_script: mkdir -p shippable/testresults
after_script: cp target/surefire-reports/* shippable/testresults
